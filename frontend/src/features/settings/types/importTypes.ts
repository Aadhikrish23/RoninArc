export interface EpicGame {
  name: string;
  installPath: string;
  executable: string;
  epicId: string;
}

export interface SteamGame {
  appId: string;
  name: string;
  installPath: string;
  executable: string;
}